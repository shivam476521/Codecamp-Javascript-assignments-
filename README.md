# Codecamp-Javascript-assignments
assignment 1
const addition = (a, b) => {
    if (typeof a !== "number" || typeof b !== "number") {
        return "Unvalid data";
    } else {
	return a + b;
    }
}
